{
  "name": "pixel-office",
  "displayName": "Pixel Office",
  "description": "Real-time visual dashboard for Claude Code agents — standalone web server with pixel art office",
  "version": "0.2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/fedevgonzalez/pixel-office"
  },
  "icon": "icon.png",
  "license": "MIT",
  "scripts": {
    "build:webview": "cd webview-ui && npm run build",
    "build": "npm run build:webview",
    "compile": "npm run check-types && npm run lint && npm run build:webview",
    "watch": "cd webview-ui && npm run dev",
    "check-types": "cd webview-ui && npx tsc --noEmit",
    "lint": "cd webview-ui && npm run lint",
    "import-tileset": "tsx scripts/import-tileset-cli.ts"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@types/node": "22.x",
    "@types/pngjs": "^6.0.5",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "gifenc": "^1.0.3",
    "npm-run-all": "^4.1.5",
    "playwright": "^1.58.2",
    "pngjs": "^7.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
